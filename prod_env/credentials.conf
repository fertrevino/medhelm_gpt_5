# API credential placeholders for HELM runs.
# Replace ONLY the right-hand side values with your actual keys.
# Do NOT commit real secrets.

# REQUIRED for GPT-5 OpenAI model access
openaiApiKey: sk-REPLACE_OPENAI_KEY

# OPTIONAL providers (delete if unused)
anthropicApiKey: sk-ant-REPLACE_ANTHROPIC_KEY
platformTneApiKey: org-REPLACE_ORG_ID
